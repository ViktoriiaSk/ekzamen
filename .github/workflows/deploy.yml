name: "Terraform & Ansible Deploy"

on:
  push:
    branches: [ "main" ]

jobs:
  terraform:
    runs-on: ubuntu-latest
    env:
      TF_VAR_do_token: ${{ secrets.DO_TOKEN }}
      TF_VAR_pvt_key: ${{ secrets.SSH_PRIVATE_KEY }}
      TF_VAR_spaces_access_key: ${{ secrets.AWS_ACCESS_KEY_ID }}
      TF_VAR_spaces_secret_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform Init
        run: terraform init

      - name: Terraform Plan
        run: terraform plan -input=false

      - name: Terraform Apply
        run: terraform apply -auto-approve

      # ANSIBLE 
      - name: Prepare SSH Key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa

      - name: Install Ansible
        run: |
          sudo apt-get update
          sudo apt-get install -y ansible

      - name: Run Ansible Installation
        env:
          ANSIBLE_HOST_KEY_CHECKING: "False"
        run: |
          # ВАЖЛИВО: Переконайтеся, що в змінній secrets.VM_IP лежить 134.122.79.221
          # Кома після змінної обов'язкова!
          ansible-playbook -i "${{ secrets.VM_IP }}," ansible/playbook.yml \
          --private-key ~/.ssh/id_rsa \
          -u root \
          --extra-vars "target_ip=${{ secrets.VM_IP }}"